export const EmptySite = () => (
  <main className="page empty">
    <h1>No content yet</h1>
    <p>
      The database is running and migrated, but no published pages exist. Add one through the admin
      area, or run the content seed.
    </p>
  </main>
);
